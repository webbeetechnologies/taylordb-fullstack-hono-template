{
  "name": "@repo/server",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Hono REST API backend server",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch index.ts",
    "build": "pnpm exec esbuild index.ts --bundle --outfile=dist/index.js --format=esm --platform=node --packages=external",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.2",
    "@taylordb/query-builder": "^0.17.0",
    "hono": "^4.12.18",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "concurrently": "^9.2.1",
    "esbuild": "^0.27.2",
    "tsx": "^4.21.0",
    "typescript": "~5.9.3"
  }
}